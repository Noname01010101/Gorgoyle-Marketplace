#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ============================================
# PRE-PUSH HOOK
# ============================================
# Ensures code builds and tests pass before push
# ============================================

echo ""
echo "╔════════════════════════════════════════╗"
echo "║   🚀 Running Pre-Push Checks...       ║"
echo "╚════════════════════════════════════════╝"
echo ""
echo "⚠️  This may take a few minutes..."
echo ""

# ============================================
# STEP 1: Run Tests
# ============================================
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🧪 Step 1/3: Running all tests..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

pnpm test

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Tests failed! Please fix failing tests before pushing."
  echo ""
  exit 1
fi

echo ""
echo "✅ All tests passed!"
echo ""

# ============================================
# STEP 2: Build API
# ============================================
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔨 Step 2/3: Building API..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

pnpm build:api

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ API build failed! Please fix build errors before pushing."
  echo ""
  exit 1
fi

echo ""
echo "✅ API build successful!"
echo ""

# ============================================
# STEP 3: Build Web Frontend
# ============================================
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🎨 Step 3/3: Building web frontend..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

pnpm build:web

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Web frontend build failed! Please fix build errors before pushing."
  echo ""
  exit 1
fi

echo ""
echo "✅ Web frontend build successful!"
echo ""

# ============================================
# SUCCESS
# ============================================
echo "╔════════════════════════════════════════╗"
echo "║   ✅ All Pre-Push Checks Passed!      ║"
echo "╚════════════════════════════════════════╝"
echo ""
echo "🎉 Ready to push!"
echo ""
