## API DEVELOPMENT INSTRUCTIONS

    1. Download node 20 on the local machine
    2. clone the repository and run npm install
    3. start the database (if necessary) using the docker-compose file
    4. move what's in .env.dev to .env for development purposes
    5. seed the database using the prisma client, and set prisma up.
